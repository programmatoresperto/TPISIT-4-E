#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <sys/wait.h>
#include <sys/fcntl.h>
#include <ctype.h>  

int main(int argc, char* argv[])
{
    int pid;
    pid = fork();
    if(pid == 0){
        printf("il carattere in maiuscolo %c", toupper(argv[1][0]));
        sleep(3);
        exit(2);
    }else
    {
        wait(&pid);
    }
    printf("Mio figlio ha terminato");
    return 0;
}


/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, OCaml, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <sys/wait.h>
#include <sys/fcntl.h>
#include <ctype.h>
#include <string.h>

int main()
{
    int Ricerca(int ricercato, int numeri []){
        for(int i=0; i<sizeof(numeri); i++){
            if(ricercato == numeri[i]){
                return ricercato;
            }
        }
        return 0; 
}
        int pid;
        int numeri[] = {1, 2, 3};
        int ricercato;
        
        printf("Inserisci il numero da cercare\n");
            scanf("%d", &ricercato);
        
        pid = fork();
        
        if(pid == 0){
            
            if(Ricerca(ricercato, numeri) == 0){
                printf("Il numero non è presente nell'array\n");
            }else{
                printf("Il numero è presente nell'aray");
            }
        }
        

    return 0;
}


/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, OCaml, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

int main(int argc, char*argv[2])
{
    char buffer[10];
    int pid;
    int p0p1[2];
    
    pipe(p0p1);
    
    pid = fork();
    
    if(pid == 0){
        char stringa[] = {"stringa"};
        close(p0p1[0]);
        write(p0p1[1], stringa, sizeof(stringa));
    
        exit (0);
    }

    
    close(p0p1[1]);
    read(p0p1[0], buffer, sizeof(buffer));
    
    printf("La stringa riportata è %s", buffer);
    return 0;
}



/******************************************************************************

Welcome to GDB Online.
  GDB online is an online compiler and debugger tool for C, C++, Python, PHP, Ruby,
  C#, OCaml, VB, Perl, Swift, Prolog, Javascript, Pascal, COBOL, HTML, CSS, JS
  Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <sys/wait.h>
#include <sys/fcntl.h>
#include <ctype.h>

int main()
{
	int p0p1[2], p1p0[2];
	int pid;
	int numeri[3];
	int somma;

	pipe(p0p1);
	pipe(p1p0);

	pid = fork();

	if(pid > 0) {
		close(p0p1[0]);
		close(p1p0[1]);
		numeri[0] = 3;
		numeri[1] = 4;
		numeri[2] = 5;

		write(p0p1[1], numeri, sizeof(numeri));

		read(p1p0[0], (void *)&somma, sizeof(somma));
		printf("Padre la somma: %d", somma);

	} else if(pid == 0) {
		close(p0p1[1]);
		close(p1p0[0]);

		read(p0p1[0], numeri, sizeof(numeri));
		for(int i=0; i<3; i++) {
			somma = somma + numeri[i];
		}

		write(p1p0[1], (void *)&somma, sizeof(somma));
	}




	return 0;
}


/******************************************************************************

Welcome to GDB Online.
  GDB online is an online compiler and debugger tool for C, C++, Python, PHP, Ruby, 
  C#, OCaml, VB, Perl, Swift, Prolog, Javascript, Pascal, COBOL, HTML, CSS, JS
  Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <sys/wait.h>
#include <sys/fcntl.h>
#include <ctype.h>
#include <string.h>

int main()
{
    int pid1, p1p2[2];
    int numero;
    
    pipe(p1p2);
    
    pid1=fork();
    
    if(pid1 == 0){
        close(p1p2[0]);
        printf("Inserisci un numero da tastiera\n");
        scanf("%d", &numero);
        
        write(p1p2[1], &numero, sizeof(numero));
        
        exit(0);
    }
    
    pid1 = fork();
    
    if(pid1 == 0){
        
        close(p1p2[1]);
        char quadrato[10];
        read(p1p2[0], (void *)&numero, sizeof(numero));
        close (p1p2[0]);
        numero = numero * numero;
        sprintf(quadrato, "%d", numero);
        write(1, quadrato, sizeof(quadrato));
        exit(0);
    }
    
    wait(&pid1);
    wait(&pid1);
    

    return 0;
}
